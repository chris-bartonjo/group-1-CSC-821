# Run the notebooks in VS Code
notebook
ipykernel

# Export a notebook to PDF or Word with tools/nb_export.py
nbconvert
pikepdf
pypandoc_binary        # bundles pandoc, used for Word export

# Used by some labs (e.g. FFT plots, Radix Sort charts)
numpy
matplotlib

# For PDF export you also need a browser already installed on your computer:
#   Google Chrome or Microsoft Edge
