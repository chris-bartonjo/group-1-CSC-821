# CSC 821 — Group 1 — environment variable template
# These algorithm labs need no secrets or API keys.
# If a lab ever needs configuration, add it here, copy this file to ".env",
# and load it with python-dotenv. The real .env is git-ignored.
#
# EXAMPLE_SETTING=value
